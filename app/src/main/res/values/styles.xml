<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="android:actionBarStyle">@style/MyActionBar</item>
        <item name="actionBarStyle">@style/MyActionBar</item>          <!--non era nemmeno male avere un tema a scala di grigi però, sai?-->
        <!-- Customize your theme here. -->
    </style>

    <style name="ButtonText" parent="android:Holo.SegmentedButton">
        <item name="android:drawable">@color/mainapp</item>
        <item name="android:textColor">@color/white</item>
    </style>

    <!-- ActionBar styles -->
    <style name="MyActionBar"
        parent="@style/Widget.AppCompat.Light.ActionBar.Solid.Inverse">
        <item name="android:background">@color/mainapp</item>
        <item name="background">@color/mainapp</item>
    </style>

    <style name="CheckBoxTheme" parent="Theme.AppCompat">
        <item name="colorAccent">@color/mainapp</item>
        <item name="android:textColorSecondary">@color/mainapp</item>
        <item name="android:textColor">@android:color/black</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="DialogTheme" parent="Theme.AppCompat.Light.Dialog">
        <item name="colorAccent">@color/mainapp</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />
    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>
